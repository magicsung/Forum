
<table class="table">
  <% @posts.each do |p| %>
    <tr>
      <td>
        <%= p.title %>
      </td>
      <td>
        <%= p.content %>
      </td>
      <td>
        <%= link_to "show", post_path(p), :class => "btn btn-info" %>
      </td>
    </tr>
  <% end %>
  
</table>